import React from 'react'
import type { PostT } from '../lib/types'

interface Props {
  post: PostT
}

const PostDetail: React.FC<Props> = ({post}): JSX.Element => {
  return (
    <div>
      
    </div>
  )
}

export default PostDetail
